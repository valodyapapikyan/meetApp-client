export enum REQUEST_NAMES {
  unassigned='unassigned',
  attendeeEvent='attendeeEvent',
  createEvent='createEvent',
  deleteEvent='deleteEvent',
  updateEvent='updateEvent'

}

export enum REQUEST_STATUS {
  unassigned,
  success,
  error
}
